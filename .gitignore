# GitHub rejects regular Git files larger than 100 MB.
CubiclePal_MVP_v0/桌宠功能展示.mp4

# Local videos and generated media.
*.mp4
*.avi
*.mov
*.mkv
*.wmv

# Model checkpoints and generated ML outputs.
*.pt
*.pth
*.onnx
*.ckpt
*.jsonl

# Vendored third-party clone, kept locally but never versioned
# (it has its own .git, so committing it only produced a broken gitlink).
RM-QIQI-vision-main/src/ros-foxglove-bridge/

# Python / cache files.
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/

# Java / Node build outputs.
target/
build/
dist/
node_modules/

# OS and editor noise.
.DS_Store
Thumbs.db
.idea/
.vscode/
